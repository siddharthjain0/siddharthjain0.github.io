This is a blog post written in Markdown. Delete this file, write your own,
and update the `POSTS` list in `index.html`.

## Headings use two hashes

Ordinary text is just text, with **bold**, *italic*, and
[links](https://arxiv.org) working the way you'd expect.

Math renders inline, like $\hbar\omega \gg k_B T$, and as displayed
equations:

$$
H = -\sum_{\langle ij \rangle} J_{ij}\,\sigma^z_i \sigma^z_j
    - \sum_i h_i\,\sigma^x_i
$$

- Bullet lists look like this
- One dash per item

Code blocks use three backticks:

```python
import numpy as np
rng = np.random.default_rng(0)
```

For figures, put the image file in the repo and reference it:

![What the figure shows](../figure.png)
